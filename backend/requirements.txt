# Core web framework
flask==3.1.0
flask-cors==5.0.1
gunicorn==23.0.0

# Environment configuration
python-dotenv==1.1.0

# HTTP client (for Ollama API calls)
requests==2.32.3

# PDF processing
PyMuPDF==1.25.4

# Text splitting
langchain-text-splitters==0.3.6

# Vector embeddings (CPU-only version)
sentence-transformers==3.4.1

# Vector similarity search (CPU-only version)
faiss-cpu==1.10.0

# Scientific computing (dependency for faiss/sentence-transformers)
numpy==2.2.3
